import React from 'react';

interface IShowPictures {
    link: string;
}

const ShowPictures = ({}:IShowPictures) => {
    return (
        <div>

        </div>
    );
};

export default ShowPictures;