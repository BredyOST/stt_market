import React from 'react';
import cls from './logo.module.scss';

const Logo = () => {
    return (
        <div  className={cls.wrapper}>
            STT
        </div>
    );
};

export default Logo;